[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "gensplines"
version = "0.1.0"
description = "Generative Spline Fields with Persistent Curve Memory"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
